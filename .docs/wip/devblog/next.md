# Devblog — what the next posts should be

The owner's queue, in the owner's words, one dated note per line. `/devblog` reads it before choosing an
angle: open notes are binding on the research and angle passes, on top of any `--brief`. When a memo answers a
note, the coordinator appends ` — used by devblog-YYYY-MM-DD` and leaves the note in place, so this file is
also the record of what was asked and when it was answered. Edit freely; the coordinator only appends.

- 2026-09-13 — "I want the next post to really be more technical and the story about the horde swarm to counterbalance this one a bit."
